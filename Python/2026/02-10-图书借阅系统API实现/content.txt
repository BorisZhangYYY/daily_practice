题目：Flask 图书借阅系统 API 实现
内容：请基于 Flask 框架实现一个简单的图书借阅系统后端 API。要求包含以下功能：
1. 用户注册与登录：用户可以通过用户名和密码注册，登录后返回 JWT Token。
2. 图书管理：管理员可以添加图书（包含书名、作者、ISBN、库存数量），普通用户可以查询图书列表（支持分页）。
3. 借阅与归还：用户登录后可以借阅图书（减少库存），也可以归还图书（增加库存）。
4. 数据验证：对输入参数进行校验（如库存不能为负数，ISBN 格式校验等）。
5. 数据库设计：设计合理的数据库表结构（用户表、图书表、借阅记录表）。